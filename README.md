# CNN-X-Ray-Pneumonia-Detection
This project uses Deep Learning (DL) to detect Pneumonia from chest X-ray images. A Convolutional Neural Network (CNN) is trained to classify scans as Normal or Pneumonia. The model is evaluated with accuracy, precision, recall, F1-score, ROC curve, and PR curve. It shows how AI can support early diagnosis and medical screening
